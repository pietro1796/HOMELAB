1- nano /etc/nginx/sites-available/pve.conf
2- incollare contenuto sotto
3- ln -s /etc/nginx/sites-available/pve.conf /etc/nginx/sites-enabled/pve.conf
4- nginx -t
5- service nginx reload


server {
    listen 80;
    server_name pve.pietrohome.it;
    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name pve.pietrohome.it;

    ssl_certificate /etc/nginx/ssl/pietrohome.pem;
    ssl_certificate_key /etc/nginx/ssl/pietrohome.key;
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_prefer_server_ciphers on;
    ssl_ciphers "HIGH:!aNULL:!MD5";
        ssl_session_cache shared:SSL:10m;
    ssl_session_tickets off;

    location / {
        proxy_pass https://192.168.200.100:8006;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_ssl_protocols TLSv1.2 TLSv1.3;
        proxy_ssl_ciphers HIGH:!aNULL:!MD5;
    }
}

