PRIME CONFIGURAZIONI
1- Installare pacchetti base
      # apt get-install sudo wget curl net-tools unzip -y
2- Settare TimeZone corretto      
      # timedatectl set-timezone Europe/Rome
3- Aggiornare server
      # apt update & apt upgrade -y
